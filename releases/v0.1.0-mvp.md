# Release: v0.1.0-mvp

**Target date:** End of Sprint 04 (~Day 21 from project start)
**Status:** Release candidate — all engineering complete. Awaiting live-env deploy + 5-user test + production ship.
**Scope owner:** Solo build

## What's in — delivered

- ✅ Core tour engine (FEAT-001) — `packages/core/src/engine/Trainer.ts`.
- ✅ JSON content schema v1 + Zod validation (FEAT-002) — `packages/core/src/schema/v1.ts`.
- ✅ `data-tour` CI validator (FEAT-003) — `scripts/validate-selectors.ts`.
- ✅ React adapter (FEAT-004) — `packages/react/src/{TourProvider,useTour,useTourProgress,FirstRunTour}.ts(x)`.
- ✅ Analytics adapter interface (FEAT-005) — `packages/core/src/adapters/analytics.ts`. Concrete AI Platform sink adapter is a one-liner pending sink confirmation (see open questions).
- ✅ LocalStorage persistence (FEAT-006).
- ✅ CSS-variable theming + AI Platform theme (FEAT-007) — stub tokens; swap for real brand tokens pre-ship.
- ✅ Advanced targeting: wait-for-element, URL trigger, event trigger, `advanceOn` click/input/url/event (FEAT-008).
- ✅ Checklist widget (FEAT-009) — `TrainingChecklist`.
- ✅ Contextual `<TrainingHint>` + `HintsProvider` + hints schema (FEAT-010).

## What's NOT in — deferred

- Vue adapter (FEAT-011) — trigger: a Vue product commits.
- Admin authoring UI (FEAT-012) — trigger: author velocity < 1 tour/hour.
- Sandbox mode (FEAT-013) — trigger: a specific tour needs it.
- Segmentation (FEAT-014) — trigger: PM asks.
- A/B testing (FEAT-015) — trigger: two viable copy variants ready.
- Cross-device persistence backend — trigger: cross-device UX complaints.

## Content shipping in v0.1.0

- 1 × onboarding tour (`ai-platform-onboarding`, 6 steps).
- 1 × basic workflow tour (`ai-platform-workflows-create-project`, 4 steps).
- Additional tours (intermediate + common-task) — curriculum owner to author before ship.
- 3 × contextual hints on the most-asked-about form fields.

All content refined based on Sprint 04 user testing (see `testing/five-user-test-protocol.md`).

## Pre-ship checklist

Engineering:

- [x] All Sprint 04 features implemented.
- [x] `npm test` green across both packages.
- [x] `npm run validate:content` green.
- [x] `npm run typecheck` green.
- [ ] `npm run validate:selectors --host <ai-platform-frontend>` green (requires host repo access).

Content:

- [x] 2 tours + 3 hints authored.
- [ ] Intermediate tour authored.
- [ ] Common-task tour authored.
- [ ] All tour copy refined post-user-test.
- [ ] Theme tokens replaced with real AI Platform brand values.

Operations:

- [ ] `data-tour` attributes merged to AI Platform frontend production branch (see `tracker/sprint-01-selectors.md`).
- [ ] Analytics sink confirmed; concrete adapter wired.
- [ ] Compliance/security sign-off obtained.
- [ ] Staging deploy completed.
- [ ] 5-user usability test completed. See `testing/five-user-test-protocol.md`.
- [ ] Manual acceptance suite green in QA. See `testing/acceptance-criteria.md`.
- [ ] Deploy checklist green. See `releases/deploy-checklist.md`.

Post-ship (day 0 → day 7):

- [ ] Analytics verification suite green in production. See `testing/analytics-verification.md`.
- [ ] Support team briefed.
- [ ] Internal announcement posted.

## Success metrics (measured 7 days post-launch)

| Metric | Target | Notes |
| --- | --- | --- |
| Onboarding tour completion rate | ≥ 60% | Of users who saw it |
| Median time to complete onboarding | ≤ 4 min | Per `estimatedMinutes` |
| `tour_error` rate | ≤ 1% | Of `tour_started` |
| Support tickets tagged "tour" | ≤ baseline + 10% | Some new tickets are expected — new surface, new questions |
| Console errors in AI Platform | No increase | Should be zero from the SDK |

## Rollback plan

See `rollback-runbook.md`. Fast path is a revert of the AI Platform consumption PR (~10 min).

Automatic rollback triggers:
- Onboarding completion drops below 20% in the first hour.
- `tour_error` rate exceeds 5% of `tour_started`.
- Console error rate up 3x from baseline.
- Any P0 bug reported.

## Post-launch review (Day 28)

Review success metrics vs. targets. Decide on v0.2 scope based on:

- What tour content had the worst completion? Fix or replace.
- What deferred feature would move the needle most next? Promote.
- Any new products asking to adopt? Prioritize Vue adapter if yes.

## Comms

- **Internal announcement:** Short post to the org's #product-updates channel. What we shipped + what to expect + how to give feedback.
- **User-facing:** No banner. The onboarding tour is the announcement.
- **Support team:** Heads-up 24 hours before ship + one-page "how it works" doc so they can answer questions.

## Known limitations shipping in v0.1.0

- Content is bundled with the SDK; updates require a redeploy of the host product. Content-served-from-API is v0.5+.
- Checklist pill shows total count only, not completed count (reactive per-tour count in the expanded view). Full reactivity in v0.2.
- Vue support absent. Only React products can adopt.
- No cross-device sync — completed on desktop = re-seen on mobile. LocalStorage is per-browser.
- No segmentation — all users see all tours (prerequisite gating aside). Segmentation in v0.2+ if PMs ask.
