# v1.0.0-api · Launch log (simulated)

**Sprint 17 · T-256.** Drops the `-preview` suffix. Adopter Product C is the first host on the new API path — first real user of hot-updates.

## What's in the tag

- `RemoteContentSource` in `@in-app-training/sdk` (Sprint 17 T-251).
- Two new events pushed the dictionary from 11 → 13: `content_bundle_updated`, `content_bundle_update_failed` (T-252).
- Persistence uses the existing `get`/`set` under `content-bundle:<product>` keys — no interface change (T-253).
- `docs/wiring-content-api.md` gains the SDK section, config reference, and boot-block toggle (T-254).
- Adopter C onboarding tour authored at `content/adopter-c/onboarding.tour.json` (T-255).
- Both preview-tier packages (`@in-app-training/api-server`, `@in-app-training/api-client`) unchanged.

## Deploy — Adopter C on the API path

```
Day 105 19:44 UTC  RemoteContentSource enabled in reports-frontend staging
Day 105 19:45 UTC  cold-boot fetch → 200, etag W/"9b71a0e28d3f4c15", bundle version g:8a2c1e0
Day 105 19:46 UTC  60-s background refresh → 304 · quiet
Day 105 19:47 UTC  tag v1.0.0-api cut
Day 105 19:52 UTC  first authored change: step 3 body reworded, POST /content/adopter-c → 201
Day 105 19:52 UTC  reports-frontend staging received content_bundle_updated within 47 s of publish
Day 105 20:22 UTC  30-min watch clean; 0 content_bundle_update_failed events
```

## Verification checklist

- [x] `npm run build --workspaces` clean across all workspaces.
- [x] `npm run test --workspaces` — 10 new tests on `RemoteContentSource` pass.
- [x] Coverage above thresholds on core (lines still ≥ 85).
- [x] `npm run docs:events:check` — dictionary at 13 events, no drift.
- [x] Adopter C reference bundle validates against `TourSchema.array()`.
- [x] Cold-boot vs cached-boot both exercised in tests (blocking and non-blocking paths).
- [x] Schema-version mismatch, network error, validation failure, and timeout all covered by tests.

## Known limitations (deliberate, resolved in Sprint 18)

- **No reactive Trainer swap yet.** Hosts read `source.snapshot()` and pass it to `Trainer` at construction; a mid-session `content_bundle_updated` currently updates future sessions only. Filed T-260 for Sprint 18.
- **`zod-to-openapi` refactor deferred (T-261).** OpenAPI spec is still hand-written. Adopter Codegen users get a working spec — the `ContentBundle` block stays `type: object` until T-261 lands.
- **Reference `ContentStore` is still in-memory.** Persistent selection lands in Sprint 18. Adopter C's production cutover from staging happens after T-260 + the persistent store choice.

## Rollback

- Tag: revert to `v1.0.0-api-preview`. `RemoteContentSource` is only imported by hosts that opted in; reverting only affects Adopter C staging.
- Adopter C: swap back to build-shipped content in `reports-frontend` (a one-line change). No SDK-side dependency prevents this.

## Next

Sprint 18 opens the cross-product analytics prep window (Sprints 18–20 per ROADMAP), and closes T-260 (reactive swap), T-261 (zod-to-openapi), T-262 (Pins-only bundle path for Reports), and the persistent `ContentStore` selection.
