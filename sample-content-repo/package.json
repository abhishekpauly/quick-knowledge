{
  "name": "@in-app-training/example-app-content",
  "version": "0.1.0",
  "description": "Tour and hint content for the example app. Consumed by @in-app-training/sdk at runtime.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "validate": "tsx ../scripts/validate-content.ts tours",
    "validate:selectors": "tsx ../scripts/validate-selectors.ts --content tours",
    "ci": "npm run validate && npm run validate:selectors"
  },
  "peerDependencies": {
    "@in-app-training/sdk": "*"
  },
  "devDependencies": {
    "@in-app-training/sdk": "*",
    "tsx": "^4.7.0",
    "typescript": "^5.4.0"
  }
}
